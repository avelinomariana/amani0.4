/* Estrutura do Quiz */
.quiz-container {
    background-color: var(--color-light-pink); /* #F8F2F4 */
    padding: 2.5rem;
    border-radius: 12px;
}

/* Cartões Visuais de Resposta (Sintomas) */
.question-card {
    border: 1px solid var(--color-rose); /* #E9B8C5 */
    padding: 1rem;
    cursor: pointer;
    transition: all 0.2s;
}
.question-card:hover, .question-card.selected {
    background-color: var(--color-marsala); /* #A0525A */
    color: white; /* Inversão para destaque elegante */
}
/* Seção Principal do Infográfico */
.efficacy-infographic {
    border-left: 4px solid var(--color-marsala); /* Linha lateral elegante */
    padding-left: 1.5rem;
    margin-top: 3rem;
}

/* Estilo do Alerta ANVISA (Validação) */
.anvisa-claim {
    color: var(--color-marsala-dark); /* #5C3131 */
    font-size: 0.85rem;
    margin-top: 1rem;
    border-top: 1px dashed var(--color-rose);
    padding-top: 0.5rem;
}

/* Destaque da Alegação (Ícones e Atributos) */
.attribute-item {
    font-weight: 600;
    line-height: 1.8;
    color: var(--color-marsala); 
    /* Use um ícone suavemente colorido com a cor Marsala */
}
.btn-primary {
    background-color: var(--color-marsala); /* O foco principal */
    color: white;
    font-weight: bold;
    padding: 0.8rem 1.5rem;
    border: none;
    border-radius: 4px;
    transition: background-color 0.3s;
}

.btn-primary:hover {
    background-color: var(--color-marsala-dark); /* Escurece no hover */
}